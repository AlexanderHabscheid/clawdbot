# Centris Gateway — Node.js gateway for voice-to-computer control
# Railway uses this as fallback when no start command is set in railway.json
web: node openclaw.mjs gateway --allow-unconfigured --bind lan --port $PORT
